kind = "web"
previewPath = "/"
title = "My Workspace"
version = "1.0.0"
id = "artifacts/workspace"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25944

[services.development]
run = "pnpm --filter @workspace/workspace run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/workspace", "run", "build" ]
publicDir = "artifacts/workspace/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25944"
BASE_PATH = "/"
